1. Create a simple navbar component and put it in the main page, with page routing for "Main" and "Users".
2. In the userform, create the proper form functions to enable submit and register the user.
3. Create an API to fetch all users to mongodb database
4. Create a "Users" page, and display a list or table to show all the users in the database.